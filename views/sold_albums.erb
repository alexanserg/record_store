<h1>All Sales Vinyl</h1>

<h3>Sold Records</h3>

<% if @sold_albums.any? %>
  <ul>
    <% @sold_albums.each do |album| %>
      <li>
        <a href="/albums/<%= album.id %>"><%= album.name %></a>
    </li>
    <% end %>
  </ul>
<% else %>
  <p>There are currently no records to display.</p>
<% end %>

<a href="/albums/new">Add a new album</a><br>
<a href="/albums/search">Search for an album</a><br>
<p><a href="/albums">Return to album list</a></p>
